import { z } from 'zod';

export const AppInstallScopeCreateSchema = z.object({
  // Add fields based on your model
});

export const AppInstallScopeUpdateSchema = z.object({
  // Add fields based on your model
});
