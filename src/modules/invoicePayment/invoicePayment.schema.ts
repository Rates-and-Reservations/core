import { z } from 'zod';

export const InvoicePaymentCreateSchema = z.object({
  // Add fields based on your model
});

export const InvoicePaymentUpdateSchema = z.object({
  // Add fields based on your model
});
