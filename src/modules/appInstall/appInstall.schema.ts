import { z } from 'zod';

export const AppInstallCreateSchema = z.object({
  // Add fields based on your model
});

export const AppInstallUpdateSchema = z.object({
  // Add fields based on your model
});
