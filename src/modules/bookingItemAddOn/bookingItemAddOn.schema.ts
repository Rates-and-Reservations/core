import { z } from 'zod';

export const BookingItemAddOnCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingItemAddOnUpdateSchema = z.object({
  // Add fields based on your model
});
