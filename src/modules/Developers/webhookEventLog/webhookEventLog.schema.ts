import { z } from 'zod';

export const WebhookEventLogCreateSchema = z.object({
  // Add fields based on your model
});

export const WebhookEventLogUpdateSchema = z.object({
  // Add fields based on your model
});
