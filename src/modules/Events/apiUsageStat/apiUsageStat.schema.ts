import { z } from 'zod';

export const ApiUsageStatCreateSchema = z.object({
  // Add fields based on your model
});

export const ApiUsageStatUpdateSchema = z.object({
  // Add fields based on your model
});
