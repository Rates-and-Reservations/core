import { z } from 'zod';

export const MerchantAssetCreateSchema = z.object({
  // Add fields based on your model
});

export const MerchantAssetUpdateSchema = z.object({
  // Add fields based on your model
});
