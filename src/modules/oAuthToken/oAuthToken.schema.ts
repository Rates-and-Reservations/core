import { z } from 'zod';

export const OAuthTokenCreateSchema = z.object({
  // Add fields based on your model
});

export const OAuthTokenUpdateSchema = z.object({
  // Add fields based on your model
});
