import { z } from 'zod';

export const PaymentActionsCreateSchema = z.object({
  // Add fields based on your model
});

export const PaymentActionsUpdateSchema = z.object({
  // Add fields based on your model
});
