import { z } from 'zod';

export const InvoiceHistoryCreateSchema = z.object({
  // Add fields based on your model
});

export const InvoiceHistoryUpdateSchema = z.object({
  // Add fields based on your model
});
