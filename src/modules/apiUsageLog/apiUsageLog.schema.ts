import { z } from 'zod';

export const ApiUsageLogCreateSchema = z.object({
  // Add fields based on your model
});

export const ApiUsageLogUpdateSchema = z.object({
  // Add fields based on your model
});
