import { z } from 'zod';

export const ResourceAssetCreateSchema = z.object({
  // Add fields based on your model
});

export const ResourceAssetUpdateSchema = z.object({
  // Add fields based on your model
});
