import { z } from 'zod';

export const ResourceBookingConfigCreateSchema = z.object({
  // Add fields based on your model
});

export const ResourceBookingConfigUpdateSchema = z.object({
  // Add fields based on your model
});
