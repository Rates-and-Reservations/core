import { z } from 'zod';

export const RefundLogCreateSchema = z.object({
  // Add fields based on your model
});

export const RefundLogUpdateSchema = z.object({
  // Add fields based on your model
});
