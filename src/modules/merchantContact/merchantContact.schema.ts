import { z } from 'zod';

export const MerchantContactCreateSchema = z.object({
  // Add fields based on your model
});

export const MerchantContactUpdateSchema = z.object({
  // Add fields based on your model
});
