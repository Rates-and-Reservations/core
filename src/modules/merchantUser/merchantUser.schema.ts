import { z } from 'zod';

export const MerchantUserCreateSchema = z.object({
  // Add fields based on your model
});

export const MerchantUserUpdateSchema = z.object({
  // Add fields based on your model
});
