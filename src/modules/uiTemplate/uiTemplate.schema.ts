import { z } from 'zod';

export const UiTemplateCreateSchema = z.object({
  // Add fields based on your model
});

export const UiTemplateUpdateSchema = z.object({
  // Add fields based on your model
});
