import { z } from 'zod';

export const BookingItemCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingItemUpdateSchema = z.object({
  // Add fields based on your model
});
