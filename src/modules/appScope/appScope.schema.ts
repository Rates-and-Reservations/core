import { z } from 'zod';

export const AppScopeCreateSchema = z.object({
  // Add fields based on your model
});

export const AppScopeUpdateSchema = z.object({
  // Add fields based on your model
});
