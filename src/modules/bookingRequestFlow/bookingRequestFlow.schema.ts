import { z } from 'zod';

export const BookingRequestFlowCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingRequestFlowUpdateSchema = z.object({
  // Add fields based on your model
});
