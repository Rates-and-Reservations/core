import { z } from 'zod';

export const BookingActivityLogCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingActivityLogUpdateSchema = z.object({
  // Add fields based on your model
});
