import { z } from 'zod';

export const BookingRequestActionValueCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingRequestActionValueUpdateSchema = z.object({
  // Add fields based on your model
});
