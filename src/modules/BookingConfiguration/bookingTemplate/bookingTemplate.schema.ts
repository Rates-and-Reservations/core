import { z } from 'zod';

export const BookingTemplateCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingTemplateUpdateSchema = z.object({
  // Add fields based on your model
});
