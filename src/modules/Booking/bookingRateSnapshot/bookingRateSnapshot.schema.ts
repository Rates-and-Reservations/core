import { z } from 'zod';

export const BookingRateSnapshotCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingRateSnapshotUpdateSchema = z.object({
  // Add fields based on your model
});
