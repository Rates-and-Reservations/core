import { z } from 'zod';

export const BookingItemTransitionCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingItemTransitionUpdateSchema = z.object({
  // Add fields based on your model
});
