import { z } from 'zod';

export const BookingTransitionCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingTransitionUpdateSchema = z.object({
  // Add fields based on your model
});
