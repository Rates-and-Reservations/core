import { z } from 'zod';

export const CancellationRequestCreateSchema = z.object({
  // Add fields based on your model
});

export const CancellationRequestUpdateSchema = z.object({
  // Add fields based on your model
});
