import { z } from 'zod';

export const CancellationRequestTransitionCreateSchema = z.object({
  // Add fields based on your model
});

export const CancellationRequestTransitionUpdateSchema = z.object({
  // Add fields based on your model
});
