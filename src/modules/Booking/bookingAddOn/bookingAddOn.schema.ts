import { z } from 'zod';

export const BookingAddOnCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingAddOnUpdateSchema = z.object({
  // Add fields based on your model
});
