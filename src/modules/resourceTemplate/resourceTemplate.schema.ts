import { z } from 'zod';

export const ResourceTemplateCreateSchema = z.object({
  // Add fields based on your model
});

export const ResourceTemplateUpdateSchema = z.object({
  // Add fields based on your model
});
