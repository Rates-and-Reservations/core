import { z } from 'zod';

export const BookingActionCreateSchema = z.object({
  // Add fields based on your model
});

export const BookingActionUpdateSchema = z.object({
  // Add fields based on your model
});
