import { z } from 'zod';

export const WaitlistEntryTransitionCreateSchema = z.object({
  // Add fields based on your model
});

export const WaitlistEntryTransitionUpdateSchema = z.object({
  // Add fields based on your model
});
