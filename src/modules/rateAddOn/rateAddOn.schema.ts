import { z } from 'zod';

export const RateAddOnCreateSchema = z.object({
  // Add fields based on your model
});

export const RateAddOnUpdateSchema = z.object({
  // Add fields based on your model
});
